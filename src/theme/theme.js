import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2A363B',
    },
    secondary: {
      main: '#99B898 ',
    },
    tertiary: {
      main: '#E84A5F'
    },
    quaternary: {
      main: '#FF847C '
    },
    quinary: {
      main: '#FECEAB'
    }
  },
});

export default theme;
